# Copy this file to `.env` and edit values for your environment.
NTRIP_USERNAME=your_username
NTRIP_PASSWORD=your_password
VEHICLE_ID=A0
TEAM_NAME="ABC"
# GPU mode: includes GPU overlay for NVIDIA device reservation
# CPU-only mode: remove or comment out this line
# COMPOSE_FILE=docker-compose.yml:docker-compose.gpu.yml
